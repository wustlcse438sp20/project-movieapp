<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cse438.cse438_assignment2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainpageActivity"></activity>
        <activity
            android:name=".ShowArtistActivity"
            android:label="@string/title_activity_show_artist"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddtoPlaylistActivity" />
        <activity android:name=".ChoosePlaylistActivity" />
        <activity android:name=".DeleteTrackListActivity" />
        <activity
            android:name=".ShowPlaylistTracksActivity"
            android:label="@string/title_activity_show_playlist_tracks"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ShowTrackActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>